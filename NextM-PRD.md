# NextM — 产品需求说明书

**版本**: V1.3
**状态**: 交叉验证修订版
**最后更新**: 2026-05-22

---

## 1. 产品概述

### 1.1 产品定位

一款以"便捷、智能、可视化"为核心的下一代个人知识管理工具（Personal Knowledge Management, PKM），帮助用户高效采集、组织、检索和复用碎片化信息，构建个人第二大脑。

### 1.2 核心价值

- **便捷** — 极低的信息采集门槛，多端无缝同步，随时随地记录
- **智能** — AI 驱动的信息提取、自动标签、语义搜索与智能问答
- **可视化** — 知识图谱、时间线、多维视图，让信息关系一目了然

### 1.3 目标用户

| 用户类型 | 典型场景 | 核心诉求 |
|----------|---------|---------|
| 知识工作者 | 日常阅读笔记、会议记录、项目文档沉淀 | 快速记录、高效检索、多端同步 |
| 研究人员 / 学生 | 文献管理、实验数据、论文写作 | 图片提取、知识关联、引用管理 |
| 终身学习者 | 看网课/教程时截屏记录知识点、整理学习笔记 | 视频截屏一键入库、自动 OCR、知识串联 |
| 创作者 | 灵感采集、素材管理、选题规划 | 可视化浏览、智能推荐 |
| 团队协作用户 | 共享知识库、项目文档协作 | 多账号、权限管理、内容同步 |

---

## 2. 功能需求

### 2.1 功能全景图

```
┌─────────────────────────────────────────────────────────────┐
│                        用户层                                │
├─────────────────────────────────────────────────────────────┤
│  Web端  │  桌面端  │  移动端  │  浏览器插件  │  CLI终端    │
├─────────────────────────────────────────────────────────────┤
│                        功能层                                │
├───────────┬───────────┬──────────┬───────────┬─────────────┤
│   采集    │   组织    │   检索   │  可视化   │   智能体    │
├──┬──┬──┬──┼──┬──┬──┬──┼──┬──┬──┬──┼──┬──┬──┬──┼──┬──┬──┬──┤
│快 │语 │会 │视 │对 │画 │数 │大 │全 │语 │MCP │知 │高 │时 │白 │总 │标 │关 │编 │定 │
│速 │音 │议 │频 │象 │布 │据 │纲 │文 │义 │连 │识 │级 │间 │板 │结 │签 │联 │排 │时 │
│剪 │速 │录 │截 │类 │白 │库 │式 │搜 │搜 │接 │图 │画 │线 │/  │智 │智 │智 │智 │智 │
│藏 │记 │音 │屏 │型 │板 │视 │大 │索 │索 │器 │谱 │布 │  │思 │能 │能 │能 │能 │能 │
│   │   │   │   │   │   │图 │纲 │   │   │   │   │   │   │维 │体 │体 │体 │体 │体 │
├──┴──┴──┴──┴──┴──┴──┴──┴──┴──┴──┴──┴──┴──┴──┴──┴──┴──┴──┴──┤
│                       数据层                                │
├─────────────────────────────────────────────────────────────┤
│  本地存储(SQLite+Markdown) │  云端同步  │  向量数据库      │
├─────────────────────────────────────────────────────────────┤
│                    开放生态                                  │
├─────────────────────────────────────────────────────────────┤
│  MCP连接器  │  插件市场  │  API开放  │  CLI工具            │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 功能模块详情

#### 2.2.1 信息采集

| 功能 | 描述 | 优先级 |
|------|------|--------|
| **视频学习截屏** | 手机看视频/网课时截图自动导入知识库（见 2.2.5） | P0 |
| **快速剪藏** | 浏览器插件一键保存网页全文/摘要/截图 | P0 |
| **富文本笔记** | 支持 Markdown、代码块、表格、公式、待办列表 | P0 |
| **图片信息提取** | OCR 识别图片文字，自动提取结构化信息（见 2.2.6） | P0 |
| **语音速记** | 语音输入自动转文字，支持 30 分钟以上长录音，自动分段和提取待办 | P0 |
| **会议录音** | 桌面端/移动端实时会议转录，自动识别说话人，生成可分享的会议纪要 | P1 |
| **AI Link 摘要** | 保存网页链接时，自动抓取并 AI 生成内容摘要 | P1 |
| **文件导入** | 拖拽导入 PDF、Word、Excel、图片、音频文件 | P1 |
| **微信/Telegram 转发** | 通过聊天机器人转发消息直接存入知识库 | P1 |
| **系统音频捕获** | 捕获系统输出音频（在线会议、视频），自动转录为笔记 | P2 |

#### 2.2.2 信息组织

| 功能 | 描述 | 优先级 |
|------|------|--------|
| **笔记本组** | 分层级的笔记本 → 页面 → 子页面结构 | P0 |
| **对象类型系统** | 借鉴 Capacities/Anytype 的对象化建模：定义类型（书籍/论文/项目/人脉）、预设属性、关联关系 | P0 |
| **智能标签** | AI 自动生成标签，支持手动调整 | P0 |
| **双向链接 + 块级引用** | `[[Wiki 链接]]` 建立页面关联，支持块级引用（类似 Logseq），自动生成反向链接面板 | P0 |
| **数据库视图** | 类似 Notion/AppFlowy 的多视图：表格、看板、画廊、日历、时间线、列表，支持自定义字段类型（文本/标签/日期/数字/进度/关联） | P0 |
| **数据库高级功能** | 跨数据库关联(Rollup)、AND/OR 组合筛选、排序、分组、批量编辑 | P1 |
| **无限画布/白板** | 类似 AFFiNE Edgeless 模式：文档↔白板一键切换，支持便签、流程图、思维导图、自由绘图 | P1 |
| **大纲模式** | 类似 Logseq 的块级大纲，可折叠/展开层级，适合深度思考和碎片整理 | P1 |
| **属性系统** | 自定义键值对属性（如 `作者:: 张三`、`状态:: 进行中`） | P1 |
| **AI 自动组织** | 类似 Mem.ai 的自动分类与关联，无需手动整理文件夹，AI 自动构建知识结构 | P1 |
| **AI 知识库整理** | 一键清理草稿笔记为结构化内容，语义合并重复笔记，自动提取待办 | P1 |
| **模板引擎** | 预设模板（会议记录、读书笔记、项目复盘等），支持自定义 | P1 |
| **Mermaid 图表** | 代码块中直接绘制流程图、时序图、甘特图、类图 | P2 |
| **闪卡系统** | 类似 Logseq 内置的间隔重复记忆卡片，自动从笔记生成复习卡片 | P2 |

#### 2.2.3 检索与问答

| 功能 | 描述 | 优先级 |
|------|------|--------|
| **全文搜索** | 标题 + 正文全文索引，支持拼音模糊搜索 | P0 |
| **语义搜索** | 基于向量嵌入的语义相似度搜索，理解查询意图，无需精确关键词 | P0 |
| **混合搜索** | 关键词 + 语义权重融合排序（RRF 算法），兼顾精确匹配和语义理解 | P0 |
| **AI 问答** | 基于知识库上下文的对话式问答，支持追问和溯源引用 | P0 |
| **深度搜索** | 类似 Mem.ai Deep Search：跨笔记综合查询，AI 整合多源信息后给出聚合答案 | P1 |
| **MCP 连接器** | 类似 Capacities 的 MCP 协议：允许 Claude/ChatGPT/Cursor 等外部 AI 直接搜索和读写知识库 | P1 |
| **多模型切换** | 用户可选择 AI 提供商：Claude / GPT / Gemini / 本地 Ollama，不锁定单一厂商 | P1 |
| **AI Palettes** | 类似 Reflect 的 AI 命令面板：选中文本后一键重写、翻译、摘要、提取待办、生成邮件 | P1 |
| **AI 推荐搜索** | 浏览笔记时自动推荐 5 条相关内容（语义 + 关联混合排序） | P1 |
| **代码片段检索** | 识别代码语言并支持按语言/功能搜索 | P2 |
| **反向链接面板** | 每篇笔记自动显示哪些其他笔记引用了它，支持预览 | P1 |

#### 2.2.4 可视化

| 功能 | 描述 | 优先级 |
|------|------|--------|
| **知识图谱** | 基于双向链接和标签自动生成交互式知识图谱，可拖拽缩放，按标签着色 | P0 |
| **关系分析面板** | 选中节点后展示关联节点强度、最短路径、中心度分析、模块聚类 | P0 |
| **知识盲点检测** | 类似 InfraNodus：AI 分析图谱结构，识别知识盲区和有待探索的连接，推荐研究方向 | P1 |
| **高级画布** | 在画布中嵌入知识图谱节点，支持流程图、演示模式、可折叠分组（类似 Obsidian Advanced Canvas） | P1 |
| **思维导图** | 从笔记/大纲自动生成思维导图，节点可点击跳转到原笔记（类似 MindCanvas） | P1 |
| **时间线视图** | 按创建/修改时间展示笔记活动流 | P1 |
| **白板/Edgeless 模式** | 类似 AFFiNE：文档模式与白板模式一键切换，自由绘制关联图 | P1 |
| **热力图** | 展示知识活跃度，识别知识密集/稀疏区域 | P2 |
| **3D 图谱** | 三维空间知识图谱（可选切换），适合大规模节点浏览 | P2 |
| **图谱筛选** | 按标签、笔记本、时间范围、对象类型动态过滤图谱显示范围 | P1 |

#### 2.2.5 视频学习截屏（移动端核心场景）

**场景描述**：用户在手机观看网课、教程、科普视频时，遇到有价值的内容直接截屏，系统自动捕获、处理并存入知识库，无需切换应用或手动操作。

**功能列表**：

| 功能 | 描述 | 优先级 |
|------|------|--------|
| **悬浮截屏按钮** | 视频播放时屏幕边缘悬浮按钮，点击即截屏 + 入库 | P0 |
| **通知栏快捷操作** | 截图后通知栏弹出"存入知识库"快捷入口 | P0 |
| **自动截图监听** | 监听系统截屏事件，自动弹出入库确认浮窗 | P0 |
| **视频源关联** | 记录截图来源（B站/YouTube/本地视频/网课App），自动填入笔记属性 | P0 |
| **时间戳记录** | 截屏时记录视频进度时间点，后续可跳转回原位置 | P0 |
| **画质增强** | 对视频帧进行超分辨率增强、去模糊、去压缩伪影 | P1 |
| **连续截屏模式** | 长按悬浮按钮进入连拍模式，快速截取多帧，后续统一处理 | P1 |
| **批量处理队列** | 连续截图自动排队，后台逐张处理，处理完推送汇总通知 | P1 |
| **字幕/弹幕捕获** | 截屏时同时捕获视频当前字幕或弹幕内容，辅助理解 | P2 |
| **关键帧检测** | 对录屏/长视频自动检测画面切换点，去重冗余帧，只保留关键帧 | P2 |

**截屏内容自动处理 Pipeline**：

```
用户按下悬浮截屏
    │
    ▼
┌──────────────────────────┐
│  截图捕获                  │
│  ├── 系统截图事件监听       │
│  └── 应用内截图 API        │
└──────────┬───────────────┘
           ▼
┌──────────────────────────┐
│  元数据采集                │
│  ├── 当前前台 App 名称     │
│  ├── 视频标题 / 课程名称   │
│  ├── 播放时间戳            │
│  ├── 当前字幕文本（如有）   │
│  └── 截图时间              │
└──────────┬───────────────┘
           ▼
┌──────────────────────────┐
│  图像预处理                │  超分辨率增强、去压缩伪影
└──────────┬───────────────┘    拉伸校正、对比度优化
           ▼
┌──────────────────────────┐
│  OCR + 版面分析           │  文字识别 + 标题/代码/表格识别
└──────────┬───────────────┘
           ▼
┌──────────────────────────┐
│  AI 内容理解              │
│  ├── 知识点提取           │
│  ├── 自动生成标题         │
│  ├── 标签推荐             │
│  └── 关联已有笔记检测     │
└──────────┬───────────────┘
           ▼
┌──────────────────────────┐
│  知识入库                 │
│  ├── 保存截图原图(可选)   │
│  ├── 保存 OCR 文本       │
│  ├── 保存视频源 + 时间戳  │
│  ├── 建立双向链接         │
│  └── 向量化嵌入索引       │
└──────────────────────────┘
```

**多视频源支持**：

| 视频源 | 支持方式 | 元数据获取能力 |
|--------|---------|--------------|
| 系统全局（任意 App） | 监听系统截屏事件 + 辅助功能 API | 应用名称、截图时间 |
| YouTube / B站 | 应用内 WebView 桥接 / 画中画 API | 视频标题、链接、时间戳、字幕 |
| 本地视频播放器 | 应用内播放器截帧 API | 文件名、播放进度 |
| 系统录屏 | 录屏结束后批量处理视频帧 | 录屏时间段 |
| 直播 | 实时截帧 + 弹幕捕获 | 直播间号、主播、时间戳 |

**去重与质量控制**：

| 策略 | 说明 |
|------|------|
| 视觉相似度去重 | 连续截图间计算 pHash 相似度，>95% 视为重复自动丢弃 |
| 文字覆盖率过滤 | 图片中文字区域占比 <5% 视为无关截图，自动丢弃 |
| 模糊检测 | 拉普拉斯方差检测模糊图像，提醒用户重新截图 |
| 内容质量评分 | AI 评估截图信息密度，低质量截图标记为"待整理" |
| 同源合并 | 同一视频的连续截图自动合并为一篇笔记 |

**手机端交互设计**：

**方式一：悬浮按钮**
```
┌──────────────────────┐
│                       │
│    ▶ 视频播放画面      │
│                       │
│              ┌────┐  │
│              │ ✚  │ ← 半透明悬浮按钮，可拖拽位置
│              └────┘  │
│  ◀────────── ▶│      │
│                       │
│   ┌─────────────┐    │
│   │ 进度条 12:34 │    │
│   └─────────────┘    │
└──────────────────────┘
    点击悬浮按钮 → 截图 + 轻触反馈（振动 + 动画）
         ↓
    通知栏："已保存到知识库 | 点击查看"
```

**方式二：相册导入**
```
相册 → 选择截图 → 分享 → "存入知识库" (系统分享菜单)
    ↓
应用打开 → 自动识别视频来源 → 进入编辑确认页
    ↓
用户确认/补充 → 保存入库
```

**方式三：自动监听（可选开启）**
```
系统截屏 (音量- + 电源)
    ↓
应用后台监听截屏事件
    ↓
浮窗弹出："检测到截图，是否存入知识库？"
    ├── 立即保存（使用 AI 自动处理）
    └── 忽略
```

#### 2.2.6 图片信息提取

| 功能 | 描述 | 优先级 |
|------|------|--------|
| **多源输入** | 本地上传、截图粘贴、URL 图片、微信转发图片 | P0 |
| **OCR 识别** | 印刷体 + 手写体文字识别，支持中英日韩等多语言 | P0 |
| **版面分析** | 识别标题、段落、表格、列表，还原文档结构 | P0 |
| **公式识别** | LaTeX 格式输出数学公式 | P1 |
| **图表数据提取** | 从柱状图/折线图/饼图中提取数值数据 | P2 |
| **思维导图识别** | 识别图片中的思维导图结构并转为可编辑节点 | P2 |
| **白板拍照** | 白板/黑板照片内容增强 + 文字提取 | P1 |
| **名片识别** | 识别名片信息并存入联系人数据库 | P2 |
| **批量处理** | 批量导入图片统一进行 OCR 识别 | P1 |

#### 2.2.7 智能体

| 功能 | 描述 | 优先级 |
|------|------|--------|
| **总结智能体** | 自动总结笔记/网页/会议内容，生成摘要和关键要点 | P0 |
| **标签智能体** | 新笔记入库时自动分析内容并推荐标签 | P0 |
| **关联智能体** | 语义分析发现笔记间隐性关联，主动推荐建立链接 | P0 |
| **智能体通信** | 多个专业化智能体通过消息总线协作完成复杂任务（见 2.2.8） | P0 |
| **Heads Up 主动推送** | 类似 Mem.ai：打开笔记时自动推送关联笔记、任务、时间线和人物卡片 | P1 |
| **知识库整理智能体** | 一键清洗草稿笔记、语义合并重复内容、推荐结构重组方案 | P1 |
| **复习智能体** | 基于间隔重复算法，定时推送复习卡片（支持 Logseq 式笔记→卡片自动转换） | P1 |
| **定时任务智能体** | 按用户设定的时间/周期自动执行：每日知识回顾、周报生成、月度知识图谱更新 | P1 |
| **长周期自主智能体** | 跨天/跨周持续执行复杂任务：持续追踪某个主题，自动收集相关信息并生成报告 | P1 |
| **事件驱动智能体** | 监听特定事件自动触发工作流：新笔记入库→自动分类+关联→通知用户 | P2 |
| **写作智能体** | 基于已有知识辅助写作，提供素材建议，自动生成初稿 | P2 |
| **知识补全** | 分析知识图谱发现盲区，推荐需要补充的领域信息 | P2 |
| **AI 技能市场** | 用户可下载/上传第三方智能体技能包，扩展人工智能体能力 | P2 |

#### 2.2.8 智能体通信（详细设计）

**概念**：多个专业化智能体通过消息总线协作，完成复杂知识处理任务。

**架构**：

```
用户请求
    │
    ▼
┌──────────────────────┐
│   编排智能体 (Orchestrator)  │
│   - 任务分解              │
│   - 结果汇总              │
└──┬──┬──┬──┬──┬──────────┘
   │  │  │  │  │
   ▼  ▼  ▼  ▼  ▼
┌────────────────────────────────────────┐
│         智能体消息总线 (Message Bus)          │
│  - 事件驱动  - 异步通信  - 状态持久化          │
└──┬──┬──┬──┬──┬──┬──┬──┬──┬──┬────────┘
   │  │  │  │  │  │  │  │  │  │
   ▼  ▼  ▼  ▼  ▼  ▼  ▼  ▼  ▼  ▼
  采集  提取   标签   搜索   总结   推荐  复习   写作   问答
  智能体 智能体  智能体  智能体  智能体  智能体  智能体  智能体  智能体
```

**典型协作流程** — "帮我整理这篇论文并找出相关笔记"：

```
1. 用户提交论文 PDF
2. 编排智能体 → 任务分解：
   ├── 提取智能体：OCR + 版面分析，提取正文/图表/公式
   ├── 标签智能体：自动生成学科/关键词标签
   ├── 搜索智能体：在已有知识库中语义搜索关联笔记
   └── 总结智能体：生成长文摘要 + 要点列表
3. 编排智能体 → 汇总结果：
   ├── 格式化保存为笔记
   ├── 自动建立双向链接到关联笔记
   └── 返回可视化结果给用户
```

**通信协议**：

```json
// 智能体消息格式
{
  "messageId": "uuid",
  "agentId": "extraction-agent",
  "targetAgentId": "tagging-agent",
  "messageType": "task_request | task_result | query | event",
  "payload": { ... },
  "context": {
    "sessionId": "uuid",
    "noteId": "uuid",
    "userId": "uuid"
  },
  "timestamp": 1716000000000,
  "ttl": 300000
}
```

**编排策略**：

| 策略 | 说明 | 适用场景 |
|------|------|---------|
| 串行 | 智能体依次执行，前序输出为后续输入 | 图片 → OCR → 标签 |
| 并行 | 独立任务同时执行 | 同时对笔记做标签 + 总结 + 关联推荐 |
| 编排 (DAG) | 有向无环图编排，部分并行 | 复杂知识处理流水线 |
| 协商 | 多智能体讨论达成一致 | 内容分类争议、标签歧义消除 |
| 事件驱动 | 监听特定事件自动触发编排流程 | 新笔记入库 → 自动处理 → 通知 |

**多智能体长期协作**（2026年核心趋势）：

```
Day 1: 用户设定 "追踪 AI 芯片行业动态"
        ↓
采集智能体: 每日自动搜索相关新闻/论文/报告
        ↓
提取智能体: 提取关键数据和企业动态
        ↓
总结智能体: 生成每日简报
        ↓
Day 7: 关联智能体: 将本周信息与已有知识库关联
        ↓
Day 30: 写作智能体: 自动生成月度行业分析报告
        通知用户审阅
```

**MCP (Model Context Protocol) 连接器**：

兼容 MCP 标准，允许外部 AI 工具（Claude、ChatGPT、Cursor 等）通过标准化协议读写知识库。类似 Capacities 2026 年推出的 AI Chat Connectors 功能。

```json
// MCP 连接器配置
{
  "connectorId": "claude-desktop",
  "protocol": "mcp",
  "capabilities": ["search", "read", "write", "query_graph"],
  "rateLimit": 100,
  "authType": "oauth"
}
```

### 2.3 多账号与权限

| 功能 | 描述 | 优先级 |
|------|------|--------|
| **多账号登录** | 支持同时登录多个账号，一键切换 | P0 |
| **账号类型** | 支持邮箱 + 密码、Google/微信 OAuth、SSO | P0 |
| **工作区切换** | 每个账号独立的工作区，数据完全隔离 | P0 |
| **个人/团队空间** | 个人私密笔记 vs 团队共享知识库 | P1 |
| **权限级别** | 所有者 / 编辑者 / 评论者 / 只读者 | P1 |
| **分享链接** | 生成公开/加密分享链接，支持过期时间 | P1 |

### 2.4 多端同步

| 功能 | 描述 | 优先级 |
|------|------|--------|
| **实时同步** | 基于 WebSocket 的实时增量同步 | P0 |
| **离线支持** | 本地缓存完整数据，离线可读可写，上线后自动合并 | P0 |
| **冲突解决** | CRDT 算法处理多端并发编辑冲突 | P0 |
| **版本历史** | 保留笔记修改历史，支持回滚到任意版本 | P1 |
| **选择性同步** | 按笔记本/标签选择同步范围 | P1 |
| **同步状态指示** | 每笔记显示同步状态（已同步/待同步/冲突） | P1 |
| **端到端加密** | 敏感内容端到端加密同步（可选） | P2 |

### 2.5 开放生态

| 功能 | 描述 | 优先级 |
|------|------|--------|
| **MCP 协议支持** | 兼容 Model Context Protocol，允许 Claude/GPT/Cursor 等外部 AI 工具直接读写知识库 | P0 |
| **REST API** | 完整的 CRUD API，支持第三方应用集成 | P1 |
| **CLI 工具** | 类似 Obsidian CLI：命令行快速搜索、创建笔记、批量操作，支持 AI agent 集成 | P1 |
| **插件市场** | 第三方开发者上传技能包/插件，经过安全扫描后上架 | P2 |
| **Webhook** | 支持入站/出站 Webhook，连接 Zapier/Make 等自动化平台 | P2 |
| **数据导出** | 完整数据导出：Markdown + JSON + PDF，无锁定风险 | P0 |

---

## 3. 技术架构

### 3.1 整体架构

```
┌──────────────────────────────────────────────────────────┐
│                    客户端层                              │
├──────────┬──────────┬──────────┬────────────────────────┤
│  Web App │ Desktop  │  Mobile  │  Browser Extension     │
│ (React)  │ (Tauri) │ (Flutter)│  (Chrome/Firefox)      │
├──────────┴──────────┴──────────┴────────────────────────┤
│              本地层                                      │
├──────────────────────┬──────────────────────────────────┤
│  本地存储 (SQLite)    │  本地向量索引 (LanceDB)          │
│  + 文件缓存           │  + 本地 Embedding 模型 (ONNX)   │
├──────────────────────┴──────────────────────────────────┤
│  Go API 网关 (Chi Router / WebSocket)                   │
├──────────────────────────────────────────────────────────┤
│                     Go 服务端层                           │
├──────────┬──────────┬──────────┬───────────────────────┤
│ Auth     │ Sync     │ Search   │ AI Service            │
│ Service  │ Service  │ Service  │ (LLM + Embedding)     │
│ (Go)     │ (Go)     │ (Go)     │ (Go + LiteLLM)        │
├──────────┼──────────┼──────────┼───────────────────────┤
│ Object   │ CRDT     │ Meili +  │ 图片提取 Pipeline     │
│ CRUD API │ Merge    │ pgvector │ OCR → Layout → NER    │
├──────────┴──────────┴──────────┴───────────────────────┤
│   PostgreSQL  │  Redis  │  MinIO (S3)  │  Qdrant        │
└──────────────────────────────────────────────────────────┘
```

### 3.2 关键技术选型

| 层 | 技术 | 选型理由 |
|-----|------|---------|
| **后端语言** | Go 1.24+ | 单二进制部署、goroutine 原生并发适合智能体、工具链完善 |
| **API 框架** | Chi Router + net/http | stdlib 兼容、中间件链式组合、无重型框架依赖 |
| **API 协议** | REST (JSON) + gRPC (内部) | 外部 REST 标准，内部 gRPC 高效通信 |
| **Web 框架** | React + TypeScript + Tailwind | 生态成熟、组件库丰富、类型安全 |
| **桌面端** | Tauri (Rust) | 体积小(~5MB)、性能优于 Electron、安全 |
| **移动端** | Flutter | 跨平台一致体验、性能好、热重载 |
| **SQL 工具链** | SQLc + database/sql | 从 SQL 生成类型安全 Go 代码，无运行时开销 |
| **本地数据库** | SQLite (go-sqlite3 / modernc.org/sqlite) | 零配置、嵌入式、Go 原生 |
| **云端数据库** | PostgreSQL + pgvector | 关系型 + 向量搜索一体化 |
| **搜索引擎** | Meilisearch (云端) / SQLite FTS5 (本地) | 全文索引 + 中文友好 |
| **向量数据库** | LanceDB (本地) / pgvector (云端) | 嵌入式 + 可扩展向量搜索 |
| **对象存储** | MinIO (自建) / AWS S3 | 图片、附件、模型文件 |
| **AI/LLM** | LiteLLM + Claude/GPT/本地 LLM | 统一多模型网关，不锁定厂商 |
| **OCR** | PaddleOCR / Tesseract + LayoutLM | 高精度中文 OCR + 版面分析 |
| **实时同步** | CRDT (Go-Yrs FFI) + WebSocket | 无冲突合并、离线协作 |
| **消息队列** | NATS (Go Client) | 轻量、高性能 JetStream 持久化、智能体通信 |
| **任务队列** | Asynq (Redis 后端) | 延迟任务、定时调度、重试机制 |
| **数据可视化** | D3.js / vis-network / Three.js | 知识图谱、3D 图谱 |

### 3.3 图片信息提取 Pipeline

```
输入图片
    │
    ▼
┌──────────────────┐
│  预处理            │  图像增强、去噪、透视校正、二值化
└──────┬───────────┘
       ▼
┌──────────────────┐
│  OCR 文字识别      │  PaddleOCR / TrOCR — 支持中英日韩
└──────┬───────────┘
       ▼
┌──────────────────┐
│  版面分析          │  LayoutLMv3 — 识别标题/段落/表格/列表
└──────┬───────────┘
       ▼
┌──────────────────┐
│  结构化提取        │  LLM 将非结构化文本转为结构化数据
└──────┬───────────┘
       ▼
┌──────────────────┐
│  实体识别 (NER)   │  提取人名、地名、日期、专有名词
└──────┬───────────┘
       ▼
┌──────────────────┐
│  知识入库          │  向量化嵌入 + 建立链接 + 存入知识库
└──────────────────┘
```

### 3.4 同步架构

```
设备 A (在线)
  │
  ├── 本地编辑 → CRDT 变更 → Journal Log (SQLite)
  │
  ├── WebSocket (Go goroutine-per-conn) ──→ 同步服务 (Go) ──→ PostgreSQL ──→ 变更广播 ──→ 设备 B
  │                                                                               │
  └── WebSocket ←── 冲突合并 ←── CRDT ←────────────────────── 本地编辑 ←────────┘

离线场景：
  设备 A (离线)        设备 A (恢复在线)
       │                      │
  本地编辑 ──────────→ 上线检测
       │                      │
  CRDT Journal ──────→ Push 变更 → 同步服务 (Go) → Merge → 广播
                           │
                      Pull 远程变更 → 本地 CRDT Merge → SQLite 更新
```

---

## 4. 用户体验设计

### 4.1 设计原则

1. **零 friction 采集** — 从看到想法到记录下来不超过 3 步
2. **渐进式呈现** — 先简单后复杂，高级功能不干扰基础流程
3. **直觉化操作** — 拖拽、右键、快捷键、划词，符合平台惯例
4. **即时反馈** — 同步状态、AI 处理进度、操作结果实时可见
5. **暗色模式** — 完整支持亮色/暗色主题

### 4.2 核心界面布局

```
┌─────────────────────────────────────────────────────────┐
│  ▲ 侧边栏 (可折叠)    │  主内容区                        │
│  ┌────────────────┐  │  ┌────────────────────────────┐ │
│  │ 搜索框 (全局)    │  │  │  笔记标题 (Markdown 编辑区)  │ │
│  ├────────────────┤  │  │                            │ │
│  │ 📁 我的笔记本    │  │  │  # 标题                     │ │
│  │  ├ 工作笔记      │  │  │  正文内容...               │ │
│  │  ├ 个人学习      │  │  │  [[双向链接]]               │ │
│  │  ├ 项目文档      │  │  │  ![](图片)                  │ │
│  │  └ 读书笔记      │  │  │                            │ │
│  ├────────────────┤  │  └────────────────────────────┘ │
│  │ 🏷 标签云        │  │                                │
│  │  AI/AI工具       │  │  ┌────────────────────────┐   │
│  │  编程/前端       │  │  │  属性面板 (右侧)         │   │
│  │  产品设计        │  │  │  ───────────────       │   │
│  ├────────────────┤  │  │  创建: 2026-05-20       │   │
│  │ 🔗 快速链接      │  │  │  标签: AI, 知识管理      │   │
│  │  今日待办       │  │  │  📎 3 个反向链接         │   │
│  │  最近更新       │  │  └────────────────────────┘   │
│  └────────────────┘  │                                │
│                      │  ┌────────────────────────────┐ │
│  ▼ 底部 (用户)       │  │  底部面板 (可展开)           │ │
│  👤 账号1 ▼         │  │  │ 📊 知识图谱  │  📅 时间线│ │
│    账号2            │  │  └────────────────────────────┘ │
└──────────────────────┴─────────────────────────────────┘
```

### 4.3 关键交互流

#### 4.3.1 快速采集流程

```
用户操作:  Ctrl+Shift+V (全局快捷键)
    ↓
弹出"快速采集"浮窗
    ↓
三种输入方式:
  ├── 文本粘贴/输入
  ├── 截图粘贴 (自动触发 OCR)
  └── 文件拖拽
    ↓
AI 自动处理:
  ├── 智能标题生成
  ├── 标签推荐
  ├── 关联笔记推荐
  └── 保存到指定笔记本
    ↓
Toast 通知 "已保存" → 可选"查看详情"
```

#### 4.3.2 多账号切换流程

```
用户操作:  点击侧边栏底部账号头像
    ↓
弹出账号切换面板
    ├── 当前账号 ✓
    ├── 已登录账号列表 (点击切换)
    └── "添加新账号" 按钮
    ↓
切换账号 → 工作区重建 → 数据完全隔离
    ↓
桌面端:  进程切换
Web 端:  Token 切换
移动端:  应用内切换
```

#### 4.3.3 图片信息提取流程

```
用户操作:  Ctrl+V 粘贴截图 / 拖入图片
    ↓
本地 OCR 引擎实时处理
    ↓
右滑面板预览提取结果:
  ├── 原始图片 (缩略图)
  ├── 识别文字 (可编辑校正)
  ├── 版面结构 (标题/列表/表格)
  ├── 提取的实体 (人名/日期/地点)
  └── "保存为笔记" 按钮
    ↓
用户确认/编辑后保存 → 自动向量化入知识库
```

### 4.4 多端适配

| 端 | 能力 | 限制 |
|----|------|------|
| **Web** | 完整功能，知识图谱可视化，AI 设置 | 离线能力有限 |
| **桌面端** | 全局快捷键，离线完整，本地 AI | 需安装 |
| **移动端** | 视频截屏、拍照提取、语音输入、轻量编辑 | 复杂可视化受限 |
| **浏览器插件** | 一键剪藏，划词笔记 | 只读本地知识库 |
| **系统分享菜单集成** | 任意 App 选中内容 → 分享 → 存入知识库 | 需系统支持 |

---

## 5. 非功能需求

### 5.1 性能指标

| 指标 | 目标值 |
|------|--------|
| 页面首屏加载 | < 2s |
| 笔记搜索响应 | < 500ms（本地）/ < 1s（云端） |
| 图片 OCR 处理 | < 3s（单张） |
| 知识图谱渲染 | < 1s（1000 节点） |
| 同步延迟 | < 2s（同账号跨设备） |
| 离线启动 | < 3s |

### 5.2 安全要求

- 传输加密：TLS 1.3
- 存储加密：AES-256-GCM
- 密码策略：最小 8 位，支持 2FA
- API 鉴权：JWT + Refresh Token 轮换
- 数据隔离：多账号数据严格隔离，行列级权限

### 5.3 可靠性

- 服务端可用性：99.9%（月故障 < 43 分钟）
- 数据备份：每日自动备份，保留 30 天
- 数据持久性：99.9999999%（11 个 9）
- 离线数据本地加密存储，不丢失

### 5.4 兼容性

| 平台 | 最低版本 |
|------|---------|
| Chrome / Edge | 最新 2 个大版本 |
| Safari | 最新 2 个大版本 |
| Firefox | 最新 2 个大版本 |
| Windows Desktop | Windows 10+ |
| macOS Desktop | macOS 12+ |
| iOS | iOS 15+ |
| Android | Android 10+ |

### 5.5 AI 服务成本控制

| 策略 | 说明 |
|------|------|
| 本地模型优先 | OCR、Embedding 优先本地推理 |
| 缓存命中 | 相似图片/文档命中缓存跳过 AI 处理 |
| 分级模型 | 简单任务用小模型(Flash)，复杂任务用大模型(Officer) |
| Token 预算 | 每用户每日 Token 限额，提供不同付费等级 |
| 批量处理 | 延迟非实时任务到低峰期批量处理 |

---

## 6. 商业模式

### 6.1 付费层级

| 层级 | 价格 (月) | 核心权益 |
|------|----------|---------|
| **Free** | ¥0 | 1 账号，100 笔记，基础搜索，每日 10 次 AI 问答 |
| **Pro** | ¥29 | 多账号(3 个)，无限笔记，AI 无限，图片提取，知识图谱 |
| **Team** | ¥79/人 | Pro 全部 + 团队空间、权限管理、共享知识库 |
| **Self-hosted** | 一次性 ¥999 | 完整功能，私有化部署，无数据离开 |

### 6.2 差异化亮点

| # | 亮点 | 市场对标 | 差异化价值 |
|---|------|---------|-----------|
| 1 | **视频学习截屏即笔记** | 竞品空白 | 手机看网课时截屏自动入库，OCR + AI 标签 + 知识串联一步到位，全球首创 |
| 2 | **图片/白板拍照即笔记** | Notion(无)、Obsidian(需插件) | 内置完整 OCR Pipeline，拍照/截图即结构化笔记 |
| 3 | **对象类型系统** | Capacities(强) vs Notion(弱) | 类型定义 + 属性 + 关联关系，远超 Notion 的松散页面 |
| 4 | **文档↔白板双模式** | AFFiNE(强) vs Notion(无) | 写作模式与可视化思维模式一键切换，融合 Notion + Miro |
| 5 | **多智能体协作** | Mem.ai(单智能体) vs 竞品(无) | 编排+并行+事件驱动+长周期协作，远超简单 AI 问答 |
| 6 | **本地优先 + 云端同步** | Obsidian(强) vs Notion(弱) | 本地 Markdown 存储 + CRDT 云端同步，兼顾数据主权和协作 |
| 7 | **多账号原生支持** | 竞品空白 | 工作/个人多个账号一键切换，数据完全隔离 |
| 8 | **MCP 协议开放** | Capacities(领先) vs 竞品(无) | 兼容 MCP 标准，让 Claude/GPT 等 AI 直接接入知识库 |
| 9 | **多模型自由切换** | Reflect(支持) vs Obsidian(无) | 不锁死在单一 AI 厂商，用户可自由选择 Claude/GPT/本地 LLM |
| 10 | **AI 知识库整理** | Mem.ai(领先) vs 竞品(无) | 一键清洗草稿、语义合并重复、自动重构知识结构 |

---

## 7. 路线图

| 阶段 | 周期 | 核心交付 |
|------|------|---------|
| **MVP** | 第 1-3 月 | Web 端 + 快速剪藏 + 基础笔记 + 全文搜索 + 单账号 |
| **Phase 2** | 第 4-6 月 | 桌面端 + 移动端 + 多端同步 + 图片提取 + 多账号 |
| **Phase 3** | 第 7-9 月 | AI 问答 + 知识图谱 + 智能标签 + 智能体架构 |
| **Phase 4** | 第 10-12 月 | 智能体通信 + 团队空间 + API 开放 + 模板市场 |

---

## 8. 附录

### 8.1 竞品对标

| 维度 | 本产品 | Notion | Obsidian | Logseq | Mem.ai | Reflect | Capacities | Anytype | AppFlowy | AFFiNE |
|------|--------|--------|----------|--------|--------|---------|------------|---------|----------|--------|
| **本地优先** | ✅ 强 | ❌ 云端 | ✅ 强 | ✅ 强 | ❌ 云端 | ❌ 云端 | ❌ 云端 | ✅ 本地+P2P | ⚠️ 混合 | ✅ 本地优先 |
| **Markdown 存储** | ✅ 是 | ❌ 私有格式 | ✅ 是 | ✅ 是 | ❌ 私有 | ❌ 私有 | ❌ 私有 | ⚠️ 私有 | ❌ 私有 | ✅ 是 |
| **离线可用** | ✅ 完整 | ⚠️ 有限 | ✅ 完整 | ✅ 完整 | ❌ 有限 | ❌ 有限 | ⚠️ 有限 | ✅ 完整 | ✅ 完整 | ✅ 完整 |
| **双向链接** | ✅ 强 | ⚠️ 有限 | ✅ 强 | ✅ 块级 | ❌ 无 | ⚠️ AI 自动 | ✅ 强 | ✅ 强 | ❌ 无 | ⚠️ 有限 |
| **知识图谱** | ✅ 内置+AI | ❌ 无 | ✅ 内置 | ✅ 内置 | ⚠️ 有限 | ❌ 无 | ✅ 内置 | ❌ 无 | ❌ 无 | ❌ 无 |
| **数据库视图** | ✅ 多视图 | ✅ 多视图 | ⚠️ 插件 | ❌ 无 | ❌ 无 | ❌ 无 | ✅ 对象类型 | ✅ 对象类型 | ✅ 多视图 | ⚠️ 基础 |
| **文档/白板双模** | ✅ 是 | ❌ 无 | ⚠️ Canvas | ❌ 无 | ❌ 无 | ❌ 无 | ❌ 无 | ❌ 无 | ❌ 无 | ✅ 独有 |
| **视频截屏入库** | ✅ **独创** | ❌ 无 | ❌ 无 | ❌ 无 | ❌ 无 | ❌ 无 | ❌ 无 | ❌ 无 | ❌ 无 | ❌ 无 |
| **图片 OCR** | ✅ 内置 Pipeline | ❌ 无 | ⚠️ 插件 | ⚠️ 有限 | ⚠️ 有限 | ✅ 内置 | ✅ Media Analysis | ❌ 无 | ❌ 无 | ❌ 无 |
| **AI 问答** | ✅ 多模型 | ✅ 官方 | ⚠️ 插件 | ⚠️ 插件 | ✅ 原生 | ✅ 多模型 | ✅ 多模型 | ⚠️ 本地 DIY | ✅ 内置 | ✅ 内置 |
| **AI 智能体** | ✅ **多体协作** | ⚠️ 基础 | ⚠️ 插件 | ❌ 无 | ✅ 单智能体 | ❌ 无 | ❌ 无 | ⚠️ 实验 | ❌ 无 | ❌ 无 |
| **智能体通信** | ✅ DAG编排 | ❌ 无 | ❌ 无 | ❌ 无 | ❌ 无 | ❌ 无 | ❌ 无 | ❌ 无 | ❌ 无 | ❌ 无 |
| **MCP 协议** | ✅ 支持 | ❌ 无 | ⚠️ 开发中 | ❌ 无 | ❌ 无 | ❌ 无 | ✅ 已支持 | ❌ 无 | ❌ 无 | ❌ 无 |
| **端到端加密** | ✅ 可选 | ❌ 无 | ❌ 无 | ❌ 无 | ❌ 无 | ✅ 有 | ❌ 无 | ✅ 有 | ❌ 无 | ✅ 可选 |
| **多账号** | ✅ **原生** | ⚠️ 切换难 | ❌ 不支持 | ❌ 不支持 | ❌ 不支持 | ❌ 不支持 | ❌ 不支持 | ❌ 不支持 | ❌ 不支持 | ❌ 不支持 |
| **开源** | ⚠️ 可选 | ❌ | ⚠️ 部分 | ✅ 完全 | ❌ | ❌ | ❌ | ✅ 完全 | ✅ 完全 | ✅ 完全 |
| **GitHub Stars** | — | — | — | 36k+ | — | — | — | 22k+ | 67.5k | 61.8k |

### 8.2 市场趋势洞察（2026年）

基于行业调研获取的 2026 年 PKM 市场关键趋势：

**1. AI 从"功能"进化为"核心架构"**
- Notion 官方内置 GPT-4.1/Claude 4，Mem.ai 以 AI 为第一性原理
- Obsidian 4000+ 插件生态中，AI 插件下载量增速最快
- 趋势：知识管理工具正在从"编辑器"进化为"AI 操作系统"

**2. 多智能体协作成为分水岭**
- 多数竞品停留在单智能体问答层面
- 2026 年红杉预测为"长周期代理之年"：AI 可执行跨天/跨周的自主任务
- 事件驱动的工作流将成为标配

**3. 数据主权意识觉醒**
- 用户越来越重视本地优先 + Markdown 格式
- 端到端加密从"加分项"变为"基本要求"
- 自部署（Self-hosted）需求快速增长

**4. MCP 协议成为开放标准**
- Capacities 2026 率先推出 MCP Chat Connectors
- Obsidian 官方 CLI + 社区正在开发 MCP 支持
- MCP 将成为知识库与外部 AI 工具的"USB 接口"

**5. 从"文件夹思维"到"对象思维"**
- Capacities 和 Anytype 引领的对象类型系统正被市场接受
- 传统文件夹结构 → 类型定义 + 属性 + 关系网络
- 数据库视图（Notion/AppFlowy 模式）与对象类型的融合

**6. 可视化成为刚需**
- AFFiNE 文档↔白板双模式获得市场认可
- Obsidian Graph View + MindCanvas 插件下载量持续攀升
- 知识盲点检测（InfraNodus 等）成为新的差异化方向

### 8.3 术语表

| 术语 | 说明 |
|------|------|
| CRDT | Conflict-Free Replicated Data Type，无冲突复制数据类型 |
| RRF | Reciprocal Rank Fusion，倒数排序融合 |
| OCR | Optical Character Recognition，光学字符识别 |
| NER | Named Entity Recognition，命名实体识别 |
| PKM | Personal Knowledge Management，个人知识管理 |
| Embedding | 将文本转换为向量表示的技术 |
| DAG | Directed Acyclic Graph，有向无环图 |
